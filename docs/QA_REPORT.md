# QA_REPORT

## Перевірки
- npm install: FAILED (npm не знайдено в середовищі)
- npm run dev: NOT_RUN (npm не знайдено в середовищі)
- npm run check: NOT_RUN (npm не знайдено в середовищі)
- Форма та /api/lead: NOT_RUN
- prefers-reduced-motion: NOT_RUN

## Нотатки
- Виконайте `npm install` після встановлення Node.js/npm.
- Для GitHub Pages використовуйте workflow `Deploy to GitHub Pages`.
