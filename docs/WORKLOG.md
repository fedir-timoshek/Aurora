# WORKLOG

## 2026-01-17
- Створено структуру проєкту та конфіги.
- Додано основні секції landing і модальні компоненти.
- Підключено форму з валідацією та API endpoint.
- Заповнено документацію (brief, content map, design system).
- Спроба `npm install` неуспішна (npm відсутній у середовищі).
- Додано підтримку GitHub Pages (static export, workflow, basePath).
- Проєкт перенесено в папку `Aurora`, додано `.gitignore`.
- Оновлено workflow: підтримка збірки без lockfile.
- Конфіг Next переведено на `next.config.mjs` для сумісності з GitHub Actions.
- Виправлено lint помилку в модалці та оновлено `tsconfig.json` для `.next/types`.
- Виправлено build: прибрано `useSearchParams` з PresentHints, додано `metadataBase`, оновлено OG image градієнт.
- Додано локальні SVG-ілюстрації (aurora veil, cityline, mesh) та оновлено Hero.
- Замінено іконки сервісів і автосервісу на локальні SVG-ілюстрації.
- Карта в контактах завантажується автоматично; cityline не перекриває текст у hero.
- Додано преміальну sweep-анімацію для hero та карток сервісів.
- Додано анімований екран завантаження з вантажівкою та сторінку 404.
- Додано scroll animations: stagger reveal для карток і градієнтні лінії під заголовками.
- Підправлено блок FAQ: компактніші відступи та ширша акцентна лінія.
