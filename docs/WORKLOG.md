# WORKLOG

## 2026-01-17
- Створено структуру проєкту та конфіги.
- Додано основні секції landing і модальні компоненти.
- Підключено форму з валідацією та API endpoint.
- Заповнено документацію (brief, content map, design system).
- Спроба `npm install` неуспішна (npm відсутній у середовищі).
- Додано підтримку GitHub Pages (static export, workflow, basePath).
- Проєкт перенесено в папку `Aurora`, додано `.gitignore`.
- Оновлено workflow: підтримка збірки без lockfile.
- Конфіг Next переведено на `next.config.mjs` для сумісності з GitHub Actions.
